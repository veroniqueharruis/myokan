---
import Container from "@components/container.astro";

const currentPath = Astro.url.pathname.replace(/\/$/, "");

const produits = [
  { title: "Zafus", path: "/zafus/" },
  { title: "Zabutons", path: "/zabutons/" },
  { title: "Sacs à zafu", path: "/sacs-zafus/" },
  { title: "Fukusas", path: "/fukusas/" },
  { title: "Oryokis", path: "/oryokis/" },
  { title: "Sacs à kesa", path: "/kesas/" },
];

const isCreationsActive = produits.some(p => currentPath === p.path.replace(/\/$/, ""));
const isPratiqueActive = currentPath === "/pratique-zazen";
const isAProposActive = currentPath === "/a-propos";
const isContactActive = currentPath === "/contact";
---

<Container>
  <header class="py-4 border-b border-gray-100 dark:border-gray-800">
    <!-- Desktop navigation -->
    <nav class="hidden lg:flex items-center justify-between" aria-label="Navigation principale">
      <a href="/" class="text-xl shrink-0 flex items-baseline gap-2 group py-2 focus-visible:ring-2 rounded-md">
        <span class="font-medium text-gray-900 dark:text-white font-heading text-2xl tracking-normal group-hover:text-gray-700 dark:group-hover:text-gray-300 transition">Myokan</span>
        <span class="text-xs uppercase tracking-wider text-gray-600 dark:text-gray-300 font-sans border-l border-gray-300 dark:border-gray-700 pl-2 font-normal">Zafus & Artisanat Zen</span>
      </a>

      <ul class="flex items-center gap-2 text-sm font-medium">
        <!-- Dropdown Créations -->
        <li class="relative" id="creations-dropdown-container">
          <button
            type="button"
            id="creations-menu-button"
            aria-expanded="false"
            aria-haspopup="true"
            aria-controls="creations-dropdown-menu"
            class:list={[
              "flex items-center gap-1.5 px-3 py-2 rounded-md transition focus-visible:ring-2",
              isCreationsActive 
                ? "text-gray-950 dark:text-white font-semibold underline underline-offset-4 decoration-2" 
                : "text-gray-700 dark:text-gray-200 hover:text-gray-950 dark:hover:text-white hover:bg-gray-50 dark:hover:bg-gray-800"
            ]}
          >
            <span>Créations</span>
            <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5 transition-transform duration-200" id="creations-arrow" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5" />
            </svg>
          </button>
          
          <ul
            id="creations-dropdown-menu"
            class="hidden absolute left-0 top-full mt-1 w-48 py-2 bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-lg shadow-lg z-50 transition before:content-[''] before:block before:absolute before:-top-3 before:left-0 before:right-0 before:h-3"
            role="menu"
            aria-labelledby="creations-menu-button"
          >
            {produits.map((p) => {
              const active = currentPath === p.path.replace(/\/$/, "");
              return (
                <li role="none">
                  <a
                    href={p.path}
                    role="menuitem"
                    aria-current={active ? "page" : undefined}
                    class:list={[
                      "block px-4 py-2.5 text-sm transition focus-visible:bg-gray-100 dark:focus-visible:bg-gray-800",
                      active 
                        ? "text-gray-950 dark:text-white font-semibold bg-gray-50 dark:bg-gray-800/60" 
                        : "text-gray-700 dark:text-gray-200 hover:text-gray-950 dark:hover:text-white hover:bg-gray-50 dark:hover:bg-gray-800"
                    ]}
                  >
                    {p.title}
                  </a>
                </li>
              );
            })}
          </ul>
        </li>

        <!-- Pratique & Zazen -->
        <li>
          <a
            href="/pratique-zazen/"
            aria-current={isPratiqueActive ? "page" : undefined}
            class:list={[
              "px-3 py-2 rounded-md transition focus-visible:ring-2",
              isPratiqueActive 
                ? "text-gray-950 dark:text-white font-semibold underline underline-offset-4 decoration-2" 
                : "text-gray-700 dark:text-gray-200 hover:text-gray-950 dark:hover:text-white hover:bg-gray-50 dark:hover:bg-gray-800"
            ]}
          >
            Pratique & Zazen
          </a>
        </li>

        <!-- À propos -->
        <li>
          <a
            href="/a-propos/"
            aria-current={isAProposActive ? "page" : undefined}
            class:list={[
              "px-3 py-2 rounded-md transition focus-visible:ring-2",
              isAProposActive 
                ? "text-gray-950 dark:text-white font-semibold underline underline-offset-4 decoration-2" 
                : "text-gray-700 dark:text-gray-200 hover:text-gray-950 dark:hover:text-white hover:bg-gray-50 dark:hover:bg-gray-800"
            ]}
          >
            À propos
          </a>
        </li>

        <!-- Contact & Commande -->
        <li>
          <a
            href="/contact/"
            aria-current={isContactActive ? "page" : undefined}
            class:list={[
              "px-3 py-2 rounded-md transition focus-visible:ring-2",
              isContactActive 
                ? "text-gray-950 dark:text-white font-semibold underline underline-offset-4 decoration-2" 
                : "text-gray-700 dark:text-gray-200 hover:text-gray-950 dark:hover:text-white hover:bg-gray-50 dark:hover:bg-gray-800"
            ]}
          >
            Commander
          </a>
        </li>

        <!-- Dark mode toggle -->
        <li>
          <button
            type="button"
            id="theme-toggle"
            aria-label="Basculer entre thème clair et sombre"
            class="ml-2 p-2.5 rounded-lg text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white hover:bg-gray-100 dark:hover:bg-gray-800 transition min-w-[44px] min-h-[44px] flex items-center justify-center focus-visible:ring-2"
          >
            <svg id="icon-sun" class="w-5 h-5 hidden" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 3v2.25m6.364.386-1.591 1.591M21 12h-2.25m-.386 6.364-1.591-1.591M12 18.75V21m-4.773-4.227-1.591 1.591M5.25 12H3m4.227-4.773L5.636 5.636M15.75 12a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0Z" />
            </svg>
            <svg id="icon-moon" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M21.752 15.002A9.72 9.72 0 0 1 18 15.75c-5.385 0-9.75-4.365-9.75-9.75 0-1.33.266-2.597.748-3.752A9.753 9.753 0 0 0 3 11.25C3 16.635 7.365 21 12.75 21a9.753 9.753 0 0 0 9.002-5.998Z" />
            </svg>
          </button>
        </li>
      </ul>
    </nav>

    <!-- Mobile navigation bar -->
    <div class="lg:hidden">
      <div class="flex items-center justify-between">
        <a href="/" class="flex flex-col py-1 focus-visible:ring-2 rounded-md">
          <span class="font-medium text-gray-900 dark:text-white font-heading text-xl tracking-normal">Myokan</span>
          <span class="text-xs uppercase tracking-wider text-gray-600 dark:text-gray-300 font-sans font-normal">Artisanat zen · Montpellier</span>
        </a>
        
        <div class="flex items-center gap-1">
          <button
            type="button"
            id="theme-toggle-mobile"
            aria-label="Basculer le mode sombre"
            class="p-2.5 rounded-lg text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white transition min-w-[44px] min-h-[44px] flex items-center justify-center focus-visible:ring-2"
          >
            <svg id="icon-sun-mobile" class="w-5 h-5 hidden" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 3v2.25m6.364.386-1.591 1.591M21 12h-2.25m-.386 6.364-1.591-1.591M12 18.75V21m-4.773-4.227-1.591 1.591M5.25 12H3m4.227-4.773L5.636 5.636M15.75 12a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0Z" />
            </svg>
            <svg id="icon-moon-mobile" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M21.752 15.002A9.72 9.72 0 0 1 18 15.75c-5.385 0-9.75-4.365-9.75-9.75 0-1.33.266-2.597.748-3.752A9.753 9.753 0 0 0 3 11.25C3 16.635 7.365 21 12.75 21a9.753 9.753 0 0 0 9.002-5.998Z" />
            </svg>
          </button>

          <button
            type="button"
            id="mobile-menu-toggle"
            aria-expanded="false"
            aria-controls="mobile-navigation-menu"
            aria-label="Ouvrir le menu de navigation"
            class="p-2.5 text-gray-700 dark:text-gray-200 hover:text-gray-950 dark:hover:text-white rounded-lg transition min-w-[44px] min-h-[44px] flex items-center justify-center focus-visible:ring-2"
          >
            <svg id="hamburger-icon" class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5M3.75 17.25h16.5" />
            </svg>
            <svg id="close-icon" class="w-6 h-6 hidden" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
      </div>

      <!-- Mobile navigation panel -->
      <nav
        id="mobile-navigation-menu"
        class="hidden mt-4 border-t border-gray-200 dark:border-gray-800 pt-4 pb-2"
        aria-label="Navigation mobile"
      >
        <p class="px-3 pb-1 text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Créations artisanales</p>
        <ul class="mb-3 space-y-0.5">
          {produits.map((p) => {
            const active = currentPath === p.path.replace(/\/$/, "");
            return (
              <li>
                <a
                  href={p.path}
                  aria-current={active ? "page" : undefined}
                  class:list={[
                    "block px-3 py-2.5 rounded-md text-base transition",
                    active 
                      ? "text-gray-950 dark:text-white font-semibold bg-gray-100 dark:bg-gray-800" 
                      : "text-gray-700 dark:text-gray-200 hover:text-gray-950 dark:hover:text-white hover:bg-gray-50 dark:hover:bg-gray-800/60"
                  ]}
                >
                  {p.title}
                </a>
              </li>
            );
          })}
        </ul>

        <div class="border-t border-gray-200 dark:border-gray-800 pt-3 space-y-0.5">
          <a
            href="/pratique-zazen/"
            aria-current={isPratiqueActive ? "page" : undefined}
            class:list={[
              "block px-3 py-2.5 rounded-md text-base transition",
              isPratiqueActive 
                ? "text-gray-950 dark:text-white font-semibold bg-gray-100 dark:bg-gray-800" 
                : "text-gray-700 dark:text-gray-200 hover:text-gray-950 dark:hover:text-white hover:bg-gray-50 dark:hover:bg-gray-800/60"
            ]}
          >
            Pratique & Zazen (Guide débutant)
          </a>
          <a
            href="/a-propos/"
            aria-current={isAProposActive ? "page" : undefined}
            class:list={[
              "block px-3 py-2.5 rounded-md text-base transition",
              isAProposActive 
                ? "text-gray-950 dark:text-white font-semibold bg-gray-100 dark:bg-gray-800" 
                : "text-gray-700 dark:text-gray-200 hover:text-gray-950 dark:hover:text-white hover:bg-gray-50 dark:hover:bg-gray-800/60"
            ]}
          >
            À propos de Véronique Harruis
          </a>
          <a
            href="/contact/"
            aria-current={isContactActive ? "page" : undefined}
            class:list={[
              "block px-3 py-2.5 rounded-md text-base transition font-medium",
              isContactActive 
                ? "text-gray-950 dark:text-white font-semibold bg-gray-100 dark:bg-gray-800" 
                : "text-gray-700 dark:text-gray-200 hover:text-gray-950 dark:hover:text-white hover:bg-gray-50 dark:hover:bg-gray-800/60"
            ]}
          >
            Passer commande / Contact
          </a>
        </div>
      </nav>
    </div>
  </header>
</Container>

<script is:inline>
  (function () {
    // Theme toggle handling
    function syncIcons(isDark) {
      ["", "-mobile"].forEach(function (suffix) {
        var sun = document.getElementById("icon-sun" + suffix);
        var moon = document.getElementById("icon-moon" + suffix);
        if (sun) sun.classList.toggle("hidden", !isDark);
        if (moon) moon.classList.toggle("hidden", isDark);
      });
    }

    function toggleTheme() {
      var isDark = document.documentElement.classList.toggle("dark");
      localStorage.setItem("theme", isDark ? "dark" : "light");
      syncIcons(isDark);
    }

    syncIcons(document.documentElement.classList.contains("dark"));

    var btn = document.getElementById("theme-toggle");
    var btnMobile = document.getElementById("theme-toggle-mobile");
    if (btn) btn.addEventListener("click", toggleTheme);
    if (btnMobile) btnMobile.addEventListener("click", toggleTheme);

    // Desktop Dropdown
    var dropdownContainer = document.getElementById("creations-dropdown-container");
    var dropdownBtn = document.getElementById("creations-menu-button");
    var dropdownMenu = document.getElementById("creations-dropdown-menu");
    var dropdownArrow = document.getElementById("creations-arrow");
    var closeTimer = null;

    function clearCloseTimer() {
      if (closeTimer) {
        clearTimeout(closeTimer);
        closeTimer = null;
      }
    }

    function openDropdown() {
      clearCloseTimer();
      if (!dropdownMenu) return;
      dropdownMenu.classList.remove("hidden");
      dropdownBtn?.setAttribute("aria-expanded", "true");
      if (dropdownArrow) dropdownArrow.style.transform = "rotate(180deg)";
    }

    function closeDropdown() {
      clearCloseTimer();
      if (!dropdownMenu) return;
      dropdownMenu.classList.add("hidden");
      dropdownBtn?.setAttribute("aria-expanded", "false");
      if (dropdownArrow) dropdownArrow.style.transform = "rotate(0deg)";
    }

    function scheduleClose() {
      clearCloseTimer();
      closeTimer = setTimeout(function () {
        if (!dropdownContainer?.contains(document.activeElement)) {
          closeDropdown();
        }
      }, 250);
    }

    if (dropdownBtn && dropdownMenu) {
      dropdownBtn.addEventListener("click", function (e) {
        e.stopPropagation();
        var expanded = dropdownBtn.getAttribute("aria-expanded") === "true";
        if (expanded) {
          closeDropdown();
        } else {
          openDropdown();
        }
      });

      // Hover handling with smooth grace period
      dropdownContainer?.addEventListener("mouseenter", openDropdown);
      dropdownContainer?.addEventListener("mouseleave", scheduleClose);

      // Close menu when clicking outside
      document.addEventListener("click", function (e) {
        if (!dropdownContainer?.contains(e.target)) {
          closeDropdown();
        }
      });

      // Close menu when a link inside is clicked
      dropdownMenu.addEventListener("click", function (e) {
        if (e.target.closest("a")) {
          closeDropdown();
        }
      });

      // Close menu when keyboard focus leaves the dropdown container
      dropdownContainer?.addEventListener("focusout", function (e) {
        if (!dropdownContainer.contains(e.relatedTarget)) {
          closeDropdown();
        }
      });

      // Keyboard navigation
      dropdownBtn.addEventListener("keydown", function (e) {
        if (e.key === "ArrowDown" || e.key === "Down") {
          e.preventDefault();
          openDropdown();
          var firstLink = dropdownMenu.querySelector("a");
          firstLink?.focus();
        }
      });

      dropdownMenu.addEventListener("keydown", function (e) {
        var links = Array.from(dropdownMenu.querySelectorAll("a"));
        var index = links.indexOf(document.activeElement);
        if (e.key === "ArrowDown" || e.key === "Down") {
          e.preventDefault();
          var nextIndex = (index + 1) % links.length;
          links[nextIndex]?.focus();
        } else if (e.key === "ArrowUp" || e.key === "Up") {
          e.preventDefault();
          if (index <= 0) {
            dropdownBtn.focus();
          } else {
            links[index - 1]?.focus();
          }
        }
      });

      document.addEventListener("keydown", function (e) {
        if (e.key === "Escape" && !dropdownMenu.classList.contains("hidden")) {
          closeDropdown();
          dropdownBtn.focus();
        }
      });
    }

    // Mobile Navigation Menu Toggle
    var mobileToggle = document.getElementById("mobile-menu-toggle");
    var mobileNav = document.getElementById("mobile-navigation-menu");
    var hamburgerIcon = document.getElementById("hamburger-icon");
    var closeIcon = document.getElementById("close-icon");

    if (mobileToggle && mobileNav) {
      mobileToggle.addEventListener("click", function () {
        var isOpen = !mobileNav.classList.contains("hidden");
        if (isOpen) {
          mobileNav.classList.add("hidden");
          mobileToggle.setAttribute("aria-expanded", "false");
          mobileToggle.setAttribute("aria-label", "Ouvrir le menu de navigation");
          hamburgerIcon?.classList.remove("hidden");
          closeIcon?.classList.add("hidden");
        } else {
          mobileNav.classList.remove("hidden");
          mobileToggle.setAttribute("aria-expanded", "true");
          mobileToggle.setAttribute("aria-label", "Fermer le menu de navigation");
          hamburgerIcon?.classList.add("hidden");
          closeIcon?.classList.remove("hidden");
        }
      });

      document.addEventListener("keydown", function (e) {
        if (e.key === "Escape" && !mobileNav.classList.contains("hidden")) {
          mobileNav.classList.add("hidden");
          mobileToggle.setAttribute("aria-expanded", "false");
          mobileToggle.setAttribute("aria-label", "Ouvrir le menu de navigation");
          hamburgerIcon?.classList.remove("hidden");
          closeIcon?.classList.add("hidden");
          mobileToggle.focus();
        }
      });
    }
  })();
</script>
